const numbers = [
    19, 23, 4, 16, 28, 13, 31, 8, 29, 14, 6, 35, 2, 11, 17, 5, 9, 27, 12, 30,
  ];
function checkprime(num){
    for (let i=2; i<num; i++){
        if (num%i===0){
        }
    }
}  
function detectprime(arr){
if (checkprime){
    return true;
}else{
    return false
}
}
primeArray = detectprime(numbers)
console.log(math.max(primeArray))
console.log(math.min(primeArray))
console.log(math.sum(primeArray))